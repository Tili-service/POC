# POC

## Overview

This repository contains every POCs developed about Tili!

## POCs

### NodeJS

This POC is contained in `Node-Poc` subdirectory. It's a simple backend implementation without gross SQL (binding).

### C#

This POC is contained in `c-sharp-backend` subdirectory. It's a simple backend implementation with a Docker deployment and a postgresql database.
